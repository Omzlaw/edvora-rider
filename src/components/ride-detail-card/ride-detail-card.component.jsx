import Box from "@mui/material/Box";
import { useStyles } from "./useStyles";

const RideDetailCard = () => {
    return (
        <Box sx={useStyles.root}></Box>
    );
}

export default RideDetailCard;